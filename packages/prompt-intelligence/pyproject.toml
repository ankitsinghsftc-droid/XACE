[project]
name = "xace-prompt-intelligence"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.20.0",
    "pydantic>=2.0.0",
    "xace-gde",
    "xace-schema-factory",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]
