/**
 * asset_link_dialog.ts
 * Re-exports AssetLinkDialog from asset_status_panel.ts.
 * The dialog is defined alongside its panel for cohesion,
 * but the manifest lists it as a separate file.
 */
export { AssetLinkDialog } from './asset_status_panel';