1. Freeze commercial scope and launch model — Decide local-first, BYOK, hosted components, supported engines, paid tiers, license terms, update channels, and support workflow; acceptance is a signed product-scope record referenced by docs, UI, CI, and release gates.
2. Establish source-of-truth inventory — Catalog every package, adapter, script, doc, test, example, generated artifact, and archived path; acceptance is a checked-in inventory proving which paths are production, test-only, archived, generated, or external.
3. Create fake-and-skip register — Inventory every mock, stub, fake helper, skipped check, placeholder, conceptual branch, fallback, stale doc contradiction, and smoke-only proof; acceptance is every item assigned to remove, replace, isolate, or document as test-only.
4. Define production-path rules — Write enforceable rules for what may run in production versus tests, demos, and certification fixtures; acceptance is CI failing if production imports fake helpers, mock providers, naive CGS edits, silent skips, or unsupported fallbacks.
5. Lock the baseline failure list — Re-run full Rust, Python, TypeScript, certification, adapter, and documentation checks from a clean checkout; acceptance is a baseline report naming every failing command, skipped gate, and missing artifact.
6. Fix engine-adapter delta compression failures — Correct unchanged-field elimination and compression-ratio behavior in `xace-engine-adapter`; acceptance is `cargo test -p xace-engine-adapter --lib` and delta performance assertions passing cleanly.
7. Fix network desync/resync failure — Correct the failing resync acknowledgement/hash path in `xace-network-core`; acceptance is `cargo test -p xace-network-core` passing with desync, rollback, late-join, and resync tests enabled.
8. Normalize workspace membership — Decide which existing packages outside Cargo/uv/npm workspaces are production packages, test fixtures, or archived material; acceptance is manifests updated and CI covering every production package exactly once.
9. Make full workspace tests clean — Repair all failing Rust tests, doctests, feature-gated tests, and package warnings that hide real issues; acceptance is `cargo test --workspace` passing from a clean target directory.
10. Make Python tests clean — Add a single command that runs all production Python package tests and tools without touching user settings; acceptance is the command passing and producing a JUnit or JSON artifact.
11. Make Builder tests clean — Add TypeScript lint/typecheck/build/test coverage for the Builder UI and server contract surface; acceptance is `npm run build` plus the declared Builder test command passing in CI.
12. Make certification non-optional for core gates — Expand `tools/certify_launch.py` so quick and full modes publish explicit pass, fail, and skipped-check artifacts; acceptance is no silent skip and every skipped installed dependency recorded as unsupported for that run.
13. Replace stale documentation claims — Align README, claims matrix, launch map, readiness plan, UI labels, and code comments with the reality audit; acceptance is forbidden-claim checks passing and no doc claiming unproven commercial behavior.
14. Complete stub design docs or archive them — Fill or formally archive `docs/00_*` through `docs/10_*` stubs and old roadmap conflicts; acceptance is no public doc containing “Stub” or stale launch authority language.
15. Establish proof artifact schema — Define canonical JSON schemas for determinism, SGC, prompt, provider, network, adapter, benchmark, fuzz, soak, security, and launch proof reports; acceptance is every proof-producing tool validating against these schemas.
16. Create proof artifact retention policy — Store local and CI proof artifacts under stable paths with run IDs, machine metadata, commit SHA, command, inputs, outputs, and redacted logs; acceptance is certification failing when required artifacts are absent.
17. Enforce no silent success responses — Audit Builder, runtime, GDE, SGC, adapter, provider, import, export, and network code for fake success and silent fallback paths; acceptance is unsupported behavior failing loudly with actionable codes.
18. Remove fake SGC from production paths — Delete or isolate fake SGC wiring from prompt, certification, and Builder production execution; acceptance is real SGC binary invocation required for every accepted prompt proof.
19. Preserve fake helpers only as test fixtures — Move remaining fake SGC, mock provider, mock engine, and smoke fixtures behind test-only names and imports; acceptance is production import scanning proving they cannot run in product flows.
20. Replace naive CGS mutation fallback — Remove direct or naive Builder mutation fallback from production apply paths; acceptance is GDE-unavailable scenarios blocking visibly with no persisted CGS changes.
21. Define authoritative SGC execution contract — Specify persisted `.xace/execution_plans/*.plan.json` ownership, hashes, schema versions, migration behavior, compatibility checks, and runtime load rules; acceptance is the contract validated by schemas and tests.
22. Implement persisted SGC plan writer — Make Builder write canonical SGC plans with plan hash, compiled CGS hash, schema version, plan version, component access sets, system metadata, and proof bundle; acceptance is plan files reproducible byte-for-byte for unchanged inputs.
23. Implement strict runtime SGC plan loader — Make runtime load persisted SGC plans as the authoritative schedule instead of deriving a built-in-only phase plan; acceptance is runtime refusing to tick without a compatible plan when SGC authority is required.
24. Validate runtime plan compatibility — Add runtime checks for CGS hash, schema version, plan version, adapter protocol version, system IDs, component IDs, deterministic flags, and migration status; acceptance is mismatches fail before tick zero with actionable diagnostics.
25. Remove silent built-in system filtering — Replace built-in-only filtering with loud unsupported-system failures and explicit compatibility records; acceptance is any dropped or unknown system failing tests and producing a proof artifact.
26. Build extensible runtime system registry — Define how SGC-compiled built-in, generated, plugin, and external systems register executors and component contracts; acceptance is runtime executing at least one non-built-in generated system through the real registry.
27. Define generated system ABI — Create a deterministic system specification and execution ABI for generated gameplay systems, including inputs, reads, writes, events, RNG, errors, and rollback hooks; acceptance is ABI tests passing for multiple generated systems.
28. Compile generated systems safely — Route accepted generated gameplay systems through validation, deterministic code checks, build sandbox, SGC compilation, artifact signing, and runtime registration; acceptance is generated code never bypassing SGC or runtime validation. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Fifty-Ninth Slice for proof.
29. Add unsupported generated-system rejection — Detect unsupported APIs, nondeterministic constructs, unsafe filesystem/network access, engine-only calls, and missing rollback hooks before runtime load; acceptance is adversarial generated systems blocked with exact reasons. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Sixtieth Slice for proof.
30. Wire runtime tick to SGC schedule — Execute phases, parallel groups, system order, dependency constraints, and component hazards from the loaded SGC plan; acceptance is schedule snapshots matching the persisted plan for every tick in replay. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Sixty-First Slice for proof.
31. Implement deterministic parallel execution policy — Decide true thread-pool execution or explicitly sequential deterministic mode per group; acceptance is concurrency behavior documented, tested, benchmarked, and not described as parallel when disabled. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Sixty-Second Slice for proof.
32. Add SGC migration behavior — Support plan invalidation and regeneration across schema/template/runtime upgrades; acceptance is stale plans rejected or migrated with a recorded proof artifact and no silent downgrade. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Sixty-Third Slice for proof.
33. Add SGC runtime proof command — Create a single command proving CGS to SGC to runtime load to tick hash to replay with no fake wiring; acceptance is proof stored under `.xace/proof/sgc-runtime/<run-id>/`. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Sixty-Fourth Slice for proof.
34. Add end-to-end CGS proof test — Test CGS generation, SGC compile, runtime load, deterministic tick, replay validation, rollback failure, and adapter snapshot output in one path; acceptance is the proof passing in CI with artifacts retained. Status: complete locally with CI artifact-retention job configured; see `docs/LAUNCH_READINESS_MAP.md` Sixty-Fifth Slice for proof.
35. Define broad prompt capability matrix — Create certified supported, constrained, clarification-required, blocked, unsupported, and experimental prompt categories with examples and product wording; acceptance is Builder and docs using the same matrix. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Sixty-Sixth Slice for proof.
36. Implement prompt classifier gate — Gate every prompt against the capability matrix before mutation planning or provider calls; acceptance is easy, ambiguous, unsupported, and adversarial prompts routed correctly in automated tests. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Sixty-Seventh Slice for proof.
37. Add prompt clarification loops — Ask bounded clarification questions for ambiguous supported or constrained requests before mutation generation; acceptance is no ambiguous prompt mutates CGS without recorded user resolution. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Sixty-Eighth Slice for proof.
38. Add broad composable gameplay primitive library — Define reusable schema, system, event, input, asset, save, and network primitives for platformer, RPG, shooter, survival, puzzle, strategy, simulation, inventory, and multiplayer combat genres; acceptance is each primitive SGC-compiled and runtime-tested.
39. Implement schema generation from prompts — Generate new components, systems, events, rules, assets, and defaults through typed CGS operations, not ad hoc JSON patches; acceptance is generated schemas passing validation, SGC, runtime, replay, and rollback tests.
40. Implement generated system definitions from prompts — Produce system definitions with deterministic reads, writes, dependencies, phase, version, rollback contract, and user-facing explanation; acceptance is every accepted system entering SGC without manual repair.
41. Implement mutation planning for composite prompts — Break multi-system prompts into ordered schema/system/asset/network/save operations with rollback plan and dependency graph; acceptance is complex prompt plans previewable and either fully applied or fully rolled back.
42. Add prompt diff preview and approval — Show structured CGS, system, asset, SGC, runtime, and cost diffs before apply; acceptance is no prompt mutation persists without explicit approval or an audited test-mode override. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Sixty-Ninth Slice for proof.
43. Add prompt undo and redo — Persist reversible mutation history with transaction IDs, hashes, SGC plan links, and replay artifacts; acceptance is undo and redo restoring expected hashes across at least 50 chained mutations.
44. Add prompt rollback recovery — On compile, runtime, replay, adapter, or provider failure, restore pre-apply CGS and runtime state; acceptance is failure tests leaving no partial CGS, plan, snapshot, or UI success state. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Seventieth Slice for proof.
45. Add prompt apply validation feedback — Return classifier result, diff, SGC result, runtime load result, replay result, rollback status, cost, latency, and proof links in every apply response; acceptance is Builder showing failures without generic error masking. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Seventy-First Slice for proof.
46. Build 100-prompt JSONL corpus — Cover easy, medium, advanced, ambiguous, unsupported, adversarial, and genre-diverse prompts across platformer, RPG, shooter, survival, puzzle, strategy, inventory, simulation, multiplayer combat, and hybrid games; acceptance is corpus reviewed and versioned. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Seventy-Second Slice for proof.
47. Add prompt corpus benchmark tool — Implement `tools/prompt_corpus_benchmark.py` with accepted, blocked, clarified, compiled, runtime-passed, rollback-passed, cost, latency, provider, model, and reproducibility columns; acceptance is JSON and markdown reports generated. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Seventy-Third Slice for proof.
48. Define prompt launch thresholds — Set measurable thresholds for classification accuracy, compilation success, runtime success, rollback success, unsupported blocking, cost, latency, provider reliability, and reproducibility; acceptance is benchmark failing below threshold. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Seventy-Fourth Slice for proof.
49. Add long-session prompt degradation tests — Run multi-step conversational authoring sessions with context growth, edits, undo, provider failures, and stale state; acceptance is 8-hour or fixed-length sessions completing without corruption or unbounded cost.
50. Add prompt security tests — Test prompt injection, adversarial instructions, malformed model responses, unsafe mutations, hallucinated capabilities, schema corruption, and secret exfiltration; acceptance is every attack blocked or quarantined with artifacts. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Seventy-Fifth Slice for proof.
51. Enforce all LLM calls through inference adapter — Scan and test that no PIL, GDE, Builder, or tool code calls providers directly; acceptance is CI failing on direct HTTP/API provider calls outside the inference package. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Seventy-Sixth Slice for proof.
52. Add provider timeout and retry policy — Record timeout, retry, rate-limit, backoff, failure category, and final outcome for every provider call; acceptance is simulated failures producing deterministic user-facing errors and telemetry. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Seventy-Seventh Slice for proof.
53. Add provider token and cost accounting — Track prompt tokens, completion tokens, cache tokens, cost, model, tier, latency, and request ID for every call; acceptance is redacted telemetry artifacts generated for prompt benchmarks. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Seventy-Eighth Slice for proof.
54. Add provider health and stale-model policy — Require exact provider, model, base URL, and key fingerprint readiness before use; acceptance is stale, missing, invalid, or untested provider configurations blocked before PIL begins. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Seventy-Ninth Slice for proof.
55. Add real hosted-provider proof gate — Behind explicit opt-in and BYOK, run real OpenAI-compatible, Anthropic, Google, and chosen local/self-hosted provider health and prompt tests; acceptance is redacted reports with no keys or secret-shaped leaks. Status: proof gate implemented locally; live BYOK acceptance pending.
56. Gate automatic model routing on evidence — Allow automatic provider/model routing only after benchmark proof for each route; acceptance is stale or unbenchmarked model choices rejected with user-visible explanation. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Eighty-First Slice for proof.
57. Add BYOK and no-key UX flows — Implement clear Builder states for no key, invalid key, stale health proof, quota failure, rate limit, and provider outage; acceptance is each state covered by UI tests and server tests. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Eighty-Second Slice for proof.
58. Add deterministic no-LLM path for simple edits — Route trivial safe value changes through deterministic GDE classification without provider calls; acceptance is benchmark proving zero provider calls for certified simple edits. Status: complete locally; see `docs/LAUNCH_READINESS_MAP.md` Eighty-Third Slice for proof.
59. Harden cross-platform deterministic serialization — Canonicalize JSON, floats, ordering, hashes, timestamps, seeds, event order, and platform-specific behavior; acceptance is identical hashes on Windows, Linux, and macOS for the same replay.
60. Complete runtime side-channel hashing decision — Decide which RNG, event, mutation, feedback, network, save, and adapter states are hash-authoritative; acceptance is docs, tests, and replay artifacts proving included and excluded state.
61. Add cross-platform replay proof — Run deterministic replay proof on Windows, Linux, and macOS with the same CGS, SGC plan, input log, and generated systems; acceptance is matching hashes and retained artifacts.
62. Harden snapshot completeness — Include or explicitly exclude entity, component, event, mutation queue, RNG, feedback, network, save, and adapter side-effect state in snapshots; acceptance is restore tests proving hash and behavior equivalence.
63. Add engine-side side-effect rollback — Define rollback for adapter-spawned objects, playback commands, feedback queues, asset binding state, and pending engine edits; acceptance is failed runtime mutations rolling back engine-visible side effects in installed editors.
64. Add rollback fuzz tests — Fuzz CGS mutations, runtime mutations, generated systems, adapter messages, save loads, and network packets through rollback paths; acceptance is no partial state, panic, or unredacted leak after failures.
65. Add replay divergence diagnosis — Report first divergent tick, system, component, event, RNG call, mutation, input, and SGC group; acceptance is injected divergence tests producing actionable debug reports.
66. Add crash recovery for project state — Recover from interrupted CGS writes, plan writes, snapshot index writes, provider failures, runtime crashes, and Builder server crashes; acceptance is corruption-injection tests restoring last valid state.
67. Add support diagnostics bundle — Generate a redacted bundle containing versions, manifests, logs, proof links, config, adapter health, provider readiness, and reproduction commands; acceptance is one command producing a shareable artifact.
68. Add observability dashboard data model — Standardize runtime, Builder, prompt, provider, adapter, network, save, and security metrics with opt-in boundaries; acceptance is metrics documented, redacted, and queryable in tests.
69. Implement minimum tick debugger — Add timeline, pause, step, snapshot list, state diff, mutation history, event trace, and hash-mismatch display; acceptance is a user can reproduce and inspect a known divergence without reading source.
70. Add exportable debug report — Export debugger state, replay inputs, hash logs, SGC plan, mutation log, and adapter feedback into a redacted report; acceptance is reports loading back into a fresh checkout for diagnosis.
71. Add adapter-feedback diagnostics panel — Show per-engine connection, protocol version, entity application, feedback queue, asset binding, playback, and validation errors; acceptance is simulated adapter failures mapped to actionable UI messages.
72. Add entity/component/system error linking — Tie every runtime, SGC, prompt, adapter, and GDE failure to stable entity, component, system, transaction, or plan IDs when possible; acceptance is generic unknown errors below defined threshold.
73. Add advanced tick debugger time travel — Implement reverse-step and time-travel navigation over retained snapshots and input logs; acceptance is debugger moving forward and backward across at least 1,000 ticks with matching hashes.
74. Add debugger conditional breakpoints — Break on entity state, component value, event type, mutation type, system ID, RNG call, hash mismatch, and network desync; acceptance is breakpoint tests hitting exact ticks.
75. Add debugger causality graph — Show which prompt, mutation, system, event, RNG call, feedback, or network packet caused a state change; acceptance is generated report tracing a combat damage event end to end.
76. Add RNG seed trace panel — Display every deterministic RNG call per tick, system, seed, and result; acceptance is illegal RNG usage blocked and legal usage replaying identically.
77. Define bidirectional edit boundaries — Limit supported engine-originated commits to selection, focus, and primitive component defaults unless explicitly expanded; acceptance is unsupported edits visibly refused before persistence.
78. Require edit audit rows — Persist an accepted runtime audit row before any engine edit commit writes CGS; acceptance is missing, rejected, stale, or mismatched audit rows blocking commit.
79. Add edit version conflict rejection — Carry CGS hash, runtime hash, adapter sequence, schema version, and preview ID through edit commit; acceptance is stale edits rejected with recoverable UI flow.
80. Add edit preview approval undo recovery — Provide preview, approval, undo, redo, rollback, and recovery for bidirectional edits; acceptance is edit tests proving no data loss across conflicts and runtime failures.
81. Define XACE-to-engine routing rules — Specify which changes flow from schema to runtime to adapter and which engine changes may flow back; acceptance is routing matrix enforced by tests and UI warnings.
82. Harden asset reference validation — Validate all asset references, hashes, types, statuses, engine support, and missing files before runtime, export, save, and adapter handoff; acceptance is unresolved references blocked or given documented fallback.
83. Build semantic binding UI — Let creators map semantic events to animation, audio, and VFX commands per engine; acceptance is bindings producing runtime playback commands in Godot, Unity, and Unreal installed-editor tests.
84. Add engine-specific binding status — Track resolved, unresolved, unsupported, missing, and fallback status per engine and binding; acceptance is Builder surfacing status before export or runtime launch.
85. Define runtime fallback bindings — Implement deterministic fallback behavior for missing or unresolved animation, audio, VFX, prefab, and mesh bindings; acceptance is fallbacks visible, non-crashing, and never mistaken for resolved assets.
86. Verify asset packaging cleanliness — Include required asset references, binding manifests, adapter config, and no secrets in package/export outputs; acceptance is export scan failing on missing assets or secret-shaped strings.
87. Harden import marker validation — Detect Godot, Unity, and Unreal project markers without modifying source projects; acceptance is import refusing ambiguous or unsupported folders with a report.
88. Add read-only engine inventory — Inventory scenes, assets, scripts, plugins, packages, input maps, and unsupported engine-native systems as references only; acceptance is reports never claiming conversion.
89. Build manual migration wizard — Provide mapping flow from engine entities/assets/scenes to CGS semantic bindings and starter components; acceptance is wizard producing reversible mappings and a manual-work report.
90. Add reversible adapter install and uninstall — Install, update, rollback, and uninstall adapters without deleting user engine data; acceptance is before/after checks proving no data loss.
91. Add honest import report — Report imported, linked, ignored, mapped, unsupported, and manual-work items; acceptance is import output matching actual files and UI copy.
92. Rename export to adapter package handoff — Remove finished-game implication from UI, docs, API names, and reports; acceptance is forbidden wording absent and export labeled as adapter install package.
93. Add export preflight validation — Check CGS, SGC plan, runtime compatibility, adapter version, assets, bindings, secrets, and engine target before export; acceptance is export blocked until preflight passes.
94. Add engine handoff instructions — Generate engine-specific install, run, debug, rollback, and support instructions in each adapter package; acceptance is a clean external project can follow them without repository knowledge.
95. Version adapter packages — Add version, compatibility matrix, dependency declarations, install/uninstall scripts, rollback support, and checksums for Godot, Unity, and Unreal packages; acceptance is package verification passing in CI.
96. Define canonical cross-engine vertical slice — Specify one CGS-owned slice with movement, combat, health, inventory, save/load, rollback, replay, semantic bindings, animation, audio, VFX, and network-ready input; acceptance is slice fixture versioned.
97. Certify vertical slice in Godot — Run the canonical slice in an installed Godot editor with validation JSON, screenshots or video, logs, and hash report; acceptance is artifacts retained under the installed-engine proof path.
98. Certify vertical slice in Unity — Run the canonical slice in an installed Unity editor with validation JSON, screenshots or video, logs, and hash report; acceptance is artifacts retained under the installed-engine proof path.
99. Certify vertical slice in Unreal — Run the canonical slice in an installed Unreal editor with validation JSON, screenshots or video, logs, and hash report; acceptance is artifacts retained under the installed-engine proof path.
100. Compare cross-engine core hashes — Compare runtime authoritative hashes and engine validation reports across Godot, Unity, and Unreal; acceptance is portable-core hashes match or documented nonportable parts are excluded.
101. Document automatic versus manual portability — Record which gameplay, asset, binding, save, input, and engine-native elements port automatically or need manual work; acceptance is docs and UI generated from the same compatibility matrix.
102. Choose multiplayer launch topology — Select and document host/client, dedicated server, peer-to-peer, or offline-only scope and explicitly forbid unimplemented topologies; acceptance is unsupported topology attempts blocked.
103. Implement lobby lifecycle — Add create, join, leave, reconnect, late-join, ready state, player identity, host migration if supported, and session teardown for the chosen topology; acceptance is lobby tests passing with runtime and Builder UI.
104. Add session compatibility checks — Detect version, schema, SGC plan, adapter, asset, package, provider, and template mismatches before session start; acceptance is mismatch tests blocking with actionable messages.
105. Integrate multiplayer with runtime authority — Connect input synchronization, authoritative tick progression, replication, prediction, rollback, resync, and adapters into the runtime loop; acceptance is multiplayer combat slice staying deterministic.
106. Add visible multiplayer UX states — Show connecting, syncing, ready, live, desynced, reconnecting, resyncing, kicked, incompatible, and failed states; acceptance is each state covered by UI and server tests.
107. Harden malicious-input limits — Add rate limits, packet validation, sequence checks, replay protection, authority checks, and cheat guard enforcement for the chosen topology; acceptance is malicious packet tests blocked without desync or crash.
108. Add multiplayer diagnostics panel — Show peers, ticks, input buffers, latency, rollback count, resync status, packet loss, hash comparisons, and authority owner; acceptance is chaos runs producing readable per-client diagnostics.
109. Run network chaos test — Test 4 to 16 clients for 60 minutes with packet loss, jitter, reordering, disconnect, reconnect, late join, malformed input, rollback, and resync; acceptance is zero permanent desync for supported scenarios and artifacts under `.xace/proof/network-chaos/<run-id>/`.
110. Run long multi-user soak — Run multi-user networked sessions beyond chaos with saves, prompt changes where supported, adapter reconnects, and runtime restarts; acceptance is no corruption, unbounded logs, or unrecoverable desync.
111. Harden save/load authority — Define save contents for CGS, SGC plan, runtime state, snapshots, assets, bindings, provider-free metadata, and multiplayer state; acceptance is save/load/replay hash equivalence tests passing.
112. Add schema migration tooling — Migrate projects and saves across schema, template, package, runtime, and adapter versions; acceptance is old fixtures loading or failing with a migration-required report.
113. Version starter templates — Add template IDs, semantic versions, compatibility contracts, migration paths, and proof artifacts for each starter template; acceptance is every shipped template creating a passing certified project.
114. Version packages and extensions — Add dependency declarations, compatibility matrix, install/update/uninstall/rollback flow, and checksums for packages and adapters; acceptance is package manager tests proving rollback after failed update.
115. Define third-party extension API — Specify stable extension points, sandboxing, compatibility, determinism requirements, security review, and deprecation policy; acceptance is one sample extension passing validation without bypassing SGC.
116. Add team source-control workflows — Canonicalize CGS serialization, diffs, merge conflict visibility, schema branches, and lock files; acceptance is team workflow tests showing deterministic diffs and recoverable conflicts.
117. Add scale benchmarks — Benchmark 10k entities, 100 systems, 1k events per tick, snapshots, deltas, bridge, save/load, replay, rollback, SGC, prompt planning, and adapters; acceptance is JSON baselines under `benchmarks/<date>/<machine>.json`.
118. Define performance budgets — Set thresholds for runtime tick, SGC compile, prompt latency, provider cost, adapter apply, network tick, save/load, memory, and UI responsiveness; acceptance is CI failing regressions beyond budget.
119. Add fuzz harnesses — Fuzz CGS, mutation payloads, network packets, provider responses, schema versions, import data, adapter messages, serialization, package manifests, and extension metadata; acceptance is scheduled fuzz runs with crash artifacts.
120. Run 8-hour product soak — Exercise prompt apply, SGC, runtime, replay, rollback, save/load, import/export, adapters, network, provider failures, and Builder UI repeatedly; acceptance is no leak, corruption, secret leak, or unrecoverable state.
121. Set up hosted CI — Add CI with Windows, Linux, macOS, Rust, Python, TypeScript, engine-free certification, artifact retention, secret scanning, benchmark gates, and reproducible build config; acceptance is required checks blocking merges.
122. Add installed-engine CI lane — Configure opt-in or scheduled CI runners for installed Godot, Unity, and Unreal validation; acceptance is engine proof artifacts produced for every advertised engine/version.
123. Add reproducible builds — Pin toolchains, dependencies, provider test modes, engine versions, adapters, and package hashes; acceptance is clean checkout builds producing identical artifacts within defined tolerances.
124. Add release artifact signing — Sign runtime binaries, Builder packages, adapter packages, installers, checksums, and proof manifests; acceptance is install/update refusing unsigned or mismatched artifacts.
125. Add supply-chain review — Audit dependencies, licenses, vulnerabilities, vendored code, generated code, package metadata, and binary artifacts; acceptance is dependency and license reports clean or formally accepted.
126. Harden secrets and privacy — Prevent keys in source, CGS, logs, snapshots, exports, crash reports, telemetry, screenshots, fixtures, proof artifacts, and support bundles; acceptance is redaction and secret-scan tests passing across all outputs.
127. Define telemetry opt-in — Make analytics, crash reports, provider telemetry, performance metrics, and support bundles privacy-safe, explicit opt-in, inspectable, and revocable; acceptance is no network telemetry sent before consent.
128. Add backup and disaster recovery — Define project backup, restore, rollback, corrupt-file repair, lost-provider-key behavior, CI artifact retention, and hosted-service recovery if any; acceptance is disaster drills passing.
129. Engage external security review — Submit runtime, Builder, provider, credential, package, update, adapter, extension, and hosted surfaces for review; acceptance is all critical/high findings fixed or formally risk-accepted.
130. Add licensing and entitlement system — Implement activation, license validation, trial, offline grace, seat/team rules, and failure UX only if required by the commercial model; acceptance is purchase-free local workflows and paid flows behaving as documented.
131. Add billing and commercial operations — Integrate billing provider, invoices, refunds, tax, account management, entitlement sync, support escalation, and abuse handling if hosted or paid features exist; acceptance is sandbox billing tests passing end to end.
132. Add updater and release channels — Implement stable, beta, nightly, rollback, adapter update, migration warnings, and release notes; acceptance is update tests proving safe upgrade and rollback from one previous version.
133. Create external-user installation flow — Package installer or documented setup for users without repository knowledge, including prerequisites, engine setup, provider setup, project creation, verification, and recovery; acceptance is a fresh machine install passing certification.
134. Create onboarding tutorials — Build guided tutorials for prompt authoring, deterministic proof, debugger use, asset binding, import/wrap, export adapter package, multiplayer, save/load, and rollback recovery; acceptance is each tutorial validated by a non-author.
135. Create compatibility matrices — Publish supported OSes, engines, engine versions, provider models, adapters, templates, packages, multiplayer topology, and known unsupported features; acceptance is generated matrix checked against certification artifacts.
136. Add support workflow — Define support bundle creation, issue templates, triage labels, response times, incident severity, escalation, and customer communication; acceptance is support drill resolving a simulated customer blocker.
137. Run external creator usability test — Recruit target creators to install, configure, author, validate, debug, recover, and export without repository help; acceptance is tracked issues closed or deferred with explicit sign-off.
138. Define readiness scoring rubric — Define 9/10 and 10/10 evidence thresholds for core architecture, determinism, rollback, multiplayer, engine portability, prompt pipeline, UI/UX, debugging, docs, testing, performance, security, reliability, onboarding, distribution, commercial operations, demo readiness, and launch readiness; acceptance is rubric independent of implementation ownership.
139. Build readiness scoring tool — Generate scorecards from tests, proof artifacts, docs, CI, benchmarks, security, usability, and launch gate data; acceptance is scores below 9 blocking commercial-launch sign-off.
140. Independently verify readiness scores — Have an independent reviewer or separate audit run reproduce evidence for every readiness area; acceptance is every major score at 9/10 or 10/10 with reproducible commands and artifacts.
141. Prepare public demo proof one — Ship a deterministic bug-catching demo showing bad mutation, hash divergence or rollback, exact diagnosis, fix, replay, and proof artifact; acceptance is a stranger reproducing it from release docs.
142. Prepare public demo proof two — Ship a cross-engine vertical-slice demo across Godot, Unity, and Unreal with matching portable-core hashes and visible gameplay; acceptance is installed-engine artifacts and visual proof published.
143. Prepare public demo proof three — Ship a prompt-to-gameplay demo from prompt through real provider, GDE, SGC, runtime, replay, rollback, adapter mirroring, and debug report; acceptance is benchmarked prompt categories and honest unsupported boundaries shown.
144. Align all public claims to proof — Map every website, README, Builder label, tutorial, release note, screenshot, and demo claim to a proof artifact; acceptance is claim audit passing with no unsupported language.
145. Complete private-alpha gate — Run private-alpha checklist covering clean tests, one reproducible local path, no fake confidence, no known corruption path, provider readiness, recovery, and support bundle; acceptance is signed artifact with blockers closed.
146. Complete public-beta gate — Run public-beta checklist covering prompt corpus, installed-engine proof, CI, docs, onboarding, security basics, import/export honesty, debugger minimum, and usability fixes; acceptance is signed artifact with all critical issues closed.
147. Complete commercial-launch gate — Run commercial-launch checklist covering 9/10 scorecard, security review, licensing, updater, support, cross-platform replay, chaos/soak/fuzz, commercial ops, demos, and public claims; acceptance is signed release decision with rollback plan.
148. Create launch rollback plan — Define how to halt release, revoke packages, roll back updates, notify users, preserve projects, disable hosted features, and restore previous versions; acceptance is tabletop exercise passed.
149. Create incident response plan — Define severity levels, on-call ownership, customer communication, forensic artifact handling, patch release path, and postmortem process; acceptance is simulated incident completed with action items closed.
150. Cut signed commercial release candidate — Build from clean tagged source, run all gates, attach proof artifacts, sign packages, publish release notes, and freeze claims; acceptance is release candidate installable, verifiable, rollbackable, and approved for commercial distribution.
