[tool.uv.workspace]
members = [
    "packages/schema-factory",
    "packages/gde",
    "packages/prompt-intelligence",
    "packages/asset-registry",
    "packages/dcl",
    "packages/inference",
    "packages/project-system",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
