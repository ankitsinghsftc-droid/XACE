# XACE Product Claims Matrix

This file is a working claims checklist derived from
`docs/XACE_PRODUCTION_READINESS_MASTER_PLAN.md`. The master plan remains the
single execution contract; this matrix exists so docs, UI labels, and launch
copy can be checked quickly.

Commercial launch-model decisions are frozen in
`docs/XACE_COMMERCIAL_SCOPE.md`. If this matrix conflicts with that record, the
commercial scope record wins until a newer signed scope record replaces it.

## Claim Rules

- Public claims must describe proven behavior, not intended architecture.
- Smoke tests prove only the scenario they run.
- Editor-free bridge proof is not the same as installed-engine production proof.
- Fake, deterministic, or contract test paths must be named as such.
- A claim blocked by this matrix can be restored only after the master-plan proof
  gate for that capability passes.

## Product Promise Matrix

| Product Promise | Current Status | Safe Alpha Wording | Blocked Wording | Launch Gate |
| --- | --- | --- | --- | --- |
| Build gameplay systems rapidly | Partially supported | Supported templates, guarded CGS edits, certified prompt categories, classifier-gated prompt routing, approval-gated prompt persistence, local prompt corpus benchmark reports, local launch provider/runtime threshold proof, local threshold enforcement, and covered prompt-security attack artifacts for local projects | Build any gameplay system instantly | Hosted provider reliability proof and UX proof |
| Natural-language gameplay authoring | Narrow supported scenarios with a shared capability matrix, classifier gate, bounded clarification loop, prompt diff approval gate, covered prompt-apply rollback recovery, structured apply feedback, reviewed 100-prompt corpus fixture, local classifier-only and launch provider/runtime benchmark reports with redacted provider accounting artifacts, local threshold enforcement, covered prompt-security attack proof, inference-adapter provider-call boundary enforcement, local provider timeout/retry telemetry proof, local provider token/cost accounting proof, provider structured-output constraint proof, unknown CGS path hard-fail proof, focused prompt Python suite artifact, exact provider health/stale-policy proof, opt-in hosted-provider proof gate, automatic route-evidence gate, provider UX-state coverage, and deterministic no-LLM simple-edit benchmark proof | Certified supported prompt mutations with classifier-gated constrained, clarification-required, blocked, unsupported, and experimental categories; ambiguous classifier routes require recorded bounded user resolution before mutation generation; accepted mutations show structured CGS/system/asset/SGC/runtime/cost previews, require explicit approval before persistence, return classifier/diff/SGC/runtime/replay/rollback/cost/latency/proof feedback on apply responses, restore the pre-apply CGS/runtime state on covered compile/runtime/replay/adapter/provider failure paths, have a reviewed versioned 100-prompt corpus, can generate local classifier-only and launch provider/runtime JSON/Markdown benchmark reports plus redacted `xace.provider_accounting_summary.v1` provider accounting artifacts, fail the benchmark when measured local thresholds are missed, block or quarantine the checked Task 50 prompt-security attack cases with artifacts, fail CI if Builder/PIL/GDE/tool code adds direct provider execution outside `packages/inference`, record provider timeout/retry/backoff/failure-category telemetry with deterministic user-facing error codes for local simulated provider failures, export exact prompt/completion/cache token, cost, model, tier, latency, request ID, cache-hit, deterministic-shortcut, failure, and redaction accounting proof through the real `InferenceAdapter`, enforce provider-level structured mutation-transaction output through native JSON schema/response schema/tool-choice where supported and repair/quarantine validation where unsupported, reject parser-unknown CGS mutation paths as production hard failures with adversarial corpus evidence, run the focused prompt-intelligence Python suite as a one-command artifact-backed gate, block stale, missing, invalid, or untested provider settings before PIL with deterministic readiness codes and Builder `xace.provider_ux_state.v1` no-key/invalid-key/stale-proof/quota/rate-limit/outage states, route certified player-speed value edits through deterministic approval-gated GDE transactions with zero provider, provider-readiness, PIL, or LLM calls, provide an explicit BYOK live-proof command that refuses to call providers unless opted in, and block automatic provider/model choices unless fresh benchmark evidence exists for the exact route | Create any game from a prompt | Live hosted provider report plus UX proof |
| Arbitrary gameplay generation | Unsupported | Not a launch claim | Generate arbitrary gameplay systems | Years-scale proof and review |
| Deterministic runtime | Primitives implemented, live enforcement pending | Deterministic runtime foundations and isolated guard/hash/replay tests | Deterministic runtime is fully enforced live | Live guard/hash/RNG/replay wiring and 10,000 tick proof |
| Mutation safety | Runtime MutationGate apply-time rollback, local static CGS conflict analysis, Builder prompt-apply atomic recovery, state-preserving runtime CGS/SGC hot-swap, hot-swap compatibility classification/enforcement, runtime migration hooks, prompt unknown CGS path hard-fail validation, and local engine side-effect rollback bindings are implemented | Runtime MutationGate validates queued operations, applies them in deterministic order through one rollback-backed transaction path, restores captured entity/component/queue plus provided event/RNG state on apply-time failure, reports structured diagnostics, GDE/Builder pre-commit checks block static dependency cycles, state access hazards, incompatible component migrations, and generated-system ABI mismatches before CGS persistence, Builder prompt applies restore pre-apply CGS, GDE, SGC artifacts, runtime status, adapter-visible session state, and UI pending state on covered failure paths before emitting success, runtime `reload_cgs` validates requested disk version IDs then hot-swaps compatible compiled schedules at a clean tick boundary without resetting live tick, entity IDs, component rows, engine bridge state, pending inputs, or the existing hash log, and runtime hot-swap candidates are classified as additive, migratable, state-transforming, or reset-required before live mutation; additive candidates and explicitly hooked migratable component backfills can proceed; failed restore paths emit adapter side-effect rollback notices that clear spawned mirror objects, playback commands, feedback queues, pending edit previews, and asset-binding caches in Godot, Unity, and Unreal | All CGS, prompt, schema, and engine-side mutations are conflict-safe and atomic | X10-023 local engine side-effect rollback proof plus installed-engine release gate |
| Real SGC integration | CLI contract, canonical persisted-plan writer, strict runtime plan loader, runtime compatibility validation, no-silent-filtering CGS-derived checks, stale-plan migration invalidation proof, local generated-system ABI validation, local safe generated-code compile/sign/register gate with unsupported generated-system rejection, two generated executor kinds registered/executed through the real runtime registry, Builder validation, proof persistence, real-binary prompt certification, runtime schedule snapshot/replay proof for persisted SGC plans, retained local CGS-to-SGC-to-runtime tick-hash replay proof command, retained end-to-end CGS proof command, explicit deterministic sequential policy for SGC-parallel-eligible groups, and local integration tests exist; CI artifact-retention workflow is configured; plugin/external executor support and release-wide signing remain pending | SGC CLI builds, validates a CGS SystemDefinition-compatible JSON envelope, emits JSON errors, Builder validates plans before accepting them, Builder stores canonical schema-backed `.xace/execution_plans/<cgs_hash>.plan.json` artifacts with component access sets, system metadata, loadability metadata, and proof references, stores SGC proof bundles, prompt certification invokes the real SGC binary, runtime can require compatibility-checked persisted SGC plans before tick zero, stale persisted plans are rejected with `.xace/proof/sgc-migration/<cgs_hash>.json` and no silent downgrade, CGS-derived compatibility mode rejects unsupported systems with a proof artifact instead of filtering them, the runtime registry can execute supported generated systems through a deterministic ABI covering inputs, reads, writes, events, RNG, errors, and rollback hooks, generated Rust source must pass local safe compile/sign checks plus unsupported-API rejection before generated-code-backed executor metadata registers, runtime schedule snapshots and per-tick world hashes can be compared against the persisted SGC plan on replay, `tools/sgc_runtime_proof.py` can store `.xace/proof/sgc-runtime/<run-id>/` evidence for a real-binary strict runtime replay path, `tools/cgs_end_to_end_proof.py` can store `.xace/proof/cgs-e2e/<run-id>/` evidence for generated CGS creation, real SGC compilation, strict runtime load, deterministic tick replay, runtime replay validation, rollback failure restoration, and adapter snapshot output, and SGC `parallel=true` groups are currently executed under a reported `deterministic_sequential` policy with no worker threads | Real SGC production integration for arbitrary generated, plugin, or external systems | Plugin/external executor contracts, release-wide artifact signing, and remote CI green artifact review |
| Prompt pipeline | Contract/scenario proof plus shared matrix, classifier gate, clarification loop, diff approval gate, covered rollback recovery, structured apply feedback, reviewed 100-prompt corpus fixture, local benchmark report tooling with redacted provider accounting artifacts, local threshold gates, covered prompt-security attack proof, inference-adapter boundary enforcement, provider timeout/retry telemetry proof, provider token/cost accounting proof, provider structured-output constraint proof, unknown CGS path hard-fail proof, focused prompt Python suite artifact, provider health/stale-policy proof, opt-in hosted-provider proof gate, automatic route-evidence gate, provider UX-state coverage, and deterministic no-LLM simple-edit benchmark proof, and local launch provider/runtime threshold proof | Guarded prompt apply path for certified CGS mutation scenarios using the Task 35 matrix, Task 36 pre-PIL classifier, Task 37 bounded resolution record for ambiguous routes, Task 42 structured diff approval before persistence, Task 44 recovery that removes failed post-apply snapshots/plans/proofs while restoring pre-apply CGS/runtime state, Task 45 apply feedback that reports classifier, diff, SGC, runtime, replay, rollback, cost, latency, and proof links in success and failure responses, Task 46 reviewed corpus coverage across easy, medium, advanced, ambiguous, unsupported, adversarial, and genre-diverse prompts, Task 47 local reports with accepted/blocked/clarified/cost/latency/provider/model/reproducibility columns, Task 48 thresholds that fail the benchmark below the selected profile, X10-028 local launch provider/runtime profile proof that runs provider-allowed corpus rows through InferenceAdapter plus real SGC/runtime and rollback proof commands, Task 50 attack cases that are blocked before provider/mutation execution or quarantined with artifacts, Task 51 scanning/CI that rejects direct provider execution outside `packages/inference`, Task 52 retry summaries that record timeout, retry, rate-limit, backoff, failure category, final outcome, and user-facing provider error codes for local simulated provider failures, Task 53 accounting artifacts that record prompt/completion/cache tokens, cost, model, tier, latency, request ID, outcome, and redaction status for prompt benchmark/provider telemetry, X10-025 structured-output constraints that request native JSON schema/response schema/tool-choice where supported and quarantine unsupported provider output before commit, X10-026 parser validation that blocks unrecognised CGS mutation paths before production apply and pins the case in the adversarial corpus, X10-027 focused Python suite packaging that runs all prompt-intelligence tests through one artifact-backed command, Task 54 readiness artifacts that block stale, missing, invalid, or untested provider configurations before PIL, Task 55 tooling that requires explicit opt-in before live provider calls, Task 56 routing evidence that rejects stale or unbenchmarked automatic provider/model choices, Task 57 Builder/server tests that cover no-key, invalid-key, stale-proof, quota, rate-limit, and outage UX states, and Task 58 benchmark proof that certified player-speed value edits emit approval-gated GDE transactions with zero provider/provider-readiness/PIL/LLM calls | Broad AI system generation | Hosted provider reliability proof plus UX proof |
| Provider secrets and calls | Local OS credential storage, local redaction/secret-scan proof, inference-adapter boundary enforcement, provider timeout/retry telemetry proof, provider token/cost accounting proof, provider structured-output constraint proof, provider health/stale-policy proof, opt-in hosted-provider proof gate, automatic route-evidence gate, and provider UX-state coverage | API keys are stored through OS credential backends; local scans verify no key-shaped leaks in source/fixtures/artifacts; Builder/PIL/GDE/tool provider execution is blocked outside `packages/inference`; provider calls through the adapter record timeout, retry, rate-limit, backoff, failure category, final outcome, deterministic user-facing error code telemetry, redacted prompt/completion/cache token, cost, model, tier, latency, request ID, cache, deterministic, and failure accounting artifacts for local simulated paths, and structured-output requested/supported/enforced/schema/quarantine telemetry for mutation transactions; prompts are blocked before PIL unless the selected hosted provider has a matching health proof for provider, model, base URL, and key fingerprint; no-key, rejected-key, stale-proof, quota, rate-limit, and outage states surface through `xace.provider_ux_state.v1`; live hosted-provider proof requires explicit opt-in and writes a redacted report; automatic provider/model routing rejects missing, stale, invalid, or failed route benchmark evidence | Secrets are securely stored | Real cross-OS validation plus archived live hosted-provider proof |
| Multiplayer | Host/client authoritative lockstep launch topology selected, runtime tick advancement gated by `InputSynchroniser`, local retained-snapshot rollback/resimulation for authoritative late input or desync, lockstep-client prediction/reconciliation overlay proof, host/client lobby/session lifecycle proof, session compatibility mismatch gates, malicious-input limit gate, multiplayer diagnostics panel proof, network chaos proof, accelerated multi-user soak proof, network primitive smoke coverage, and visible unsupported-topology failures | Launch scope supports host/client authoritative lockstep scenarios whose runtime ticks wait/release/synthesize/reject-late through `InputSynchroniser`; explicit authoritative-correction/desync recovery can restore a retained clean-boundary runtime snapshot, resimulate through normal tick execution, validate hashes, record rollback count/restored tick, and emit adapter resync evidence; supported lockstep clients can produce read-only prediction previews from authoritative transform/velocity state, store them in a bounded `PredictionBuffer`, reconcile against authoritative post-tick state with `ReconciliationEngine`, report correction distance/mode, and compare client/server authoritative tick hashes without predicted state mutating component tables directly; `SessionManager` covers create lobby, identity-backed join, ready state, live start, leave, reconnect, late join without blocking existing live input peers, lifecycle events/status, teardown, and pre-start compatibility gates for schema, SGC plan, adapter version, assets, packages, provider-free metadata, and template IDs for the selected topology; `MaliciousInputGate` covers typed input-packet ingress with per-peer rate windows, packet validation, replay/sequence protection, signature/player/device/action/tick policy, authority checks, stable rejection reports, and two-phase cheat-guard commit so rejected packets do not enter synchronizer buffers/logs or poison accepted sequence state; `capture_multiplayer_diagnostics` and Builder's diagnostics panel expose peers, ticks, input buffers, latency, rollback count, resync status, packet loss, hash comparisons, authority ownership, and a deterministic chaos-report fixture for the selected topology; X10-043 proves 4/8/16-client deterministic network chaos, and X10-044 proves an accelerated multi-user soak with saves, supported prompt changes, adapter reconnect contracts, runtime restarts, and no unrecoverable desync/corruption for the supported scenario; offline remains local-only; dedicated-server and peer-to-peer return `XACE_NETWORK_TOPOLOGY_UNSUPPORTED` and are not launch-supported | Production-grade multiplayer, dedicated-server support, peer-to-peer/NAT traversal, matchmaking, transport authentication, asset/package download repair, broader security review, wall-clock production operations soak, or installed-engine live adapter soak beyond retained contracts | X10-043 network chaos proof; X10-044 accelerated multi-user soak proof |
| Runtime debugging | Minimum debugger, 1,000-tick hash time-travel navigation, runtime delta-compressed scrub retention, conditional breakpoint exact-hit proof, combat-damage causality graph proof, RNG seed trace proof, redacted support diagnostics bundle proof, and exportable debug report round-trip proof implemented | Builder's live tick debugger can pause, step, request runtime snapshots, render a tick/hash timeline, list snapshots, show selected-snapshot state diffs, derive mutation history and event trace rows, surface hash mismatches from runtime protocol payloads without reading source, navigate backward/forward across a retained 1,000-tick hash timeline with matching selected hashes, arm source-free conditional breakpoint presets with pause-on-hit behavior for entity state, component value, event type, mutation type, system ID, RNG call, hash mismatch, and network desync candidates, display explicit `runtime_causality_trace` DAGs that connect prompt, mutation, system, event, RNG call, feedback, and network-packet causes to a selected state-change node, display explicit `runtime_rng_trace` records that make deterministic RNG calls visible by tick, system, seed, stream position, and result while surfacing missing-field diagnostics, blocked illegal-RNG violations, and identical legal replay hashes, export a local-only redacted support diagnostics bundle with versions, manifests, logs, proof links, config, adapter health, provider readiness, and reproduction commands, and export a self-contained local-only redacted debug report that embeds debugger state, replay inputs, hash logs, persisted SGC plan, mutation log, adapter feedback, section digests, source manifests, artifact manifests, reproduction commands, and a fresh-checkout validation contract; the runtime now feeds end-of-tick `WorldSnapshot` records into a delta-compressed retention store with sparse full anchors, per-tick snapshot deltas, byte/tick-bounded pruning, retained-tick reconstruction, and `restore_retained_timeline_tick` validation through the existing authoritative `restore_world_snapshot` path; X10-045 retains a known-divergence fixture, X10-046 retains a 1,000-tick reverse/forward navigation proof, X10-047 retains memory/restore benchmark proof, X10-048 retains exact-hit breakpoint proof, X10-049 retains a combat-damage causality graph proof, X10-050 retains an RNG seed trace proof, X10-051 retains a redacted support diagnostics bundle proof, and X10-052 retains an exportable debug report round-trip proof | Installed-engine UI proof for driving authoritative scrub restore from the visible panel | Installed-engine debugger visible-panel release gate |
| Cross-engine core | Adapter and bridge proofs plus strict asset-reference preflight proof, semantic binding authoring/contract proof, per-engine binding status reporting, deterministic runtime fallback proof, a versioned canonical CGS-owned vertical-slice fixture, retained installed-Godot vertical-slice proof, retained installed-Unity vertical-slice proof, retained installed-Unreal vertical-slice proof, and retained cross-engine portable-core hash comparison exist | Gameplay-core portability through CGS, adapter protocol, creator-authored semantic event bindings for animation/audio/VFX playback commands, strict asset ref/hash/type/status/file/engine-support preflight gates, pre-runtime/handoff per-engine binding status for resolved, unresolved, unsupported, missing, and fallback states, deterministic runtime fallback metadata for missing/placeholder semantic playback assets, visible adapter fallback artifacts for animation/audio/VFX/prefab/mesh domains that report `fallback` rather than `resolved`, runtime semantic playback command tests, Godot/Unity/Unreal adapter playback-command and status-report contract checks, editor-free bridge proofs, `projects/canonical_cross_engine_vertical_slice` as the single versioned fixture covering movement, combat, health, inventory, save/load, rollback, replay, semantic bindings, animation, audio, VFX fallback, and network-ready input, Task 64 installed Godot headless proof with validation JSON, PNG evidence, logs, and hash report, Task 65 installed Unity batch-mode proof with validation JSON, PNG evidence, logs, and hash report, Task 66 installed Unreal commandlet proof with validation JSON, PNG evidence, logs, plugin build output, and hash report, and Task 67 retained comparison proving matching portable-core hash `a9852c1849ed26f5a78a0c8d47b4b8a161db77ffce277641e86ac1abceb37222` while explicitly excluding nonportable engine/visual/log/build artifacts | Full finished-game portability or installed-editor visual/package equivalence between engines | Finished-game packaging/export proof and broader engine-version matrix |
| Existing project import | Wrap/link plus read-only marker validation, reference inventory, preview-only manual migration planning, and reversible XACE adapter install/update/rollback/uninstall; gameplay migration remains guided/manual | Link an existing Godot, Unity, or Unreal project to a new XACE project after read-only marker validation; inventory scenes, assets, scripts, plugins, and input maps as references only; refuse ambiguous roots with reports; generate a preview-only manual migration plan that maps scene/entity/asset references to reversible CGS starter modes, starter actors/components, asset refs, and semantic binding candidates with file-backed manual-work reports; install, update, roll back, and uninstall XACE-owned adapter files under Godot `addons/xace`, Unity `Assets/XACE`, and Unreal `Plugins/XACE` using ownership manifests and before/after proof while preserving user engine files | Import existing Unity/Godot/Unreal gameplay automatically, auto-merge user-edited adapter files, or delete/rewrite arbitrary engine project data | Installed-editor UX proof plus approved apply workflow proof |
| Adapter package handoff | Adapter package handoff with reusable asset-reference preflight core, semantic binding command contracts, per-engine binding status reports, runtime fallback metadata, honest Builder UI/API naming, handoff manifest wording, an umbrella preflight gate before copy, and versioned package manifests for Godot/Unity/Unreal | Adapter package handoff for engine-owned integration requires target engine support, strict CGS validation, persisted SGC plan validation, runtime compatibility proof, adapter protocol/version marker checks, validated asset refs, author-authored semantic animation/audio/VFX playback bindings, pre-handoff per-engine binding status reports, local secret scanning, versioned package manifest output, compatibility matrix, dependency declarations, install/uninstall/rollback lifecycle scripts, rollback support metadata, and SHA-256 checksums before the receiving engine owns packaging/build work | Export a finished game | Package signing/update flow, installed-editor package import matrix, engine packaging, and build handoff proof |
| Non-technical creators | Not yet | Developer/creator alpha for guided local workflows | Ready for non-technical public creators | UX validation and recovery proof |
| Professional studios | Not yet | Architecture foundations under hardening | Ready for professional studios | CI/security/perf/compatibility and external proof |

## X10-034 Long-Session Claim Boundary

Safe launch wording may say that XACE has a retained fixed-length prompt
long-session degradation proof for supported prompt mutations. The retained
profile covers 240 deterministic authoring turns with context growth and
compaction, typed edits, proof-linked undo/redo, local simulated provider
failures, stale-state rejection, redacted provider accounting, bounded cost, and
real SGC/runtime replay checkpoints.

Do not claim that an unattended eight-hour hosted-provider UI session has
passed, or that hosted-provider reliability at long-session scale is proven.
Those remain live-provider/UX soak evidence tracks.

## Forbidden Public Claims

These phrases, and close variants, must not appear in public-facing docs or UI
unless they are clearly inside a "do not claim" or blocked-claims section:

- "Generate arbitrary gameplay systems."
- "Create any game from a prompt."
- "Full finished-game portability between engines."
- "Import existing Unity/Godot/Unreal gameplay automatically."
- "Export a finished game."
- "Production-grade multiplayer."
- "Deterministic runtime is fully enforced live."
- "All CGS, prompt, schema, and engine-side mutations are conflict-safe and atomic."
- "Real SGC production integration" for arbitrary generated, plugin, or external systems until those executor contracts, release-wide signing, and remote retained-CI evidence pass.
- "Secrets are securely stored."
- "Ready for non-technical public creators."
- "Ready for professional studios."

## Verification

Run:

```powershell
python tools/forbidden_claims_check.py
```

The launch certification command also runs this check.
